{
  "name": "mobile-gaming",
  "version": "1.0.0",
  "description": "Browser-based puzzle games inspired by mobile game ads",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui",
    "test:e2e:debug": "playwright test --debug"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "vite": "^5.4.0"
  }
}
